{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "https://github.com/nblmc/Data-Context/blob/master/schema.json",
	"title": "LMEC Data Description Schema",
	"type": "object",
	"properties": {
		"coreCitation": {
			"description": "Basic information about the collection or dataset",
			"type": "object",
			"properties": {
				"$id": {
					"type": "string",
					"description": "Unique identifier for the collection or dataset",
					"format": "uri"
				},
				"title": {
					"description": "Full name of the dataset or project",
					"type": "string"
				},
				"recordType": {
					"description": "Distinguishes between records describing collections, and records describing singular datasets",
					"type": "string",
					"enum": ["Collection", "Dataset"]
				},
				"hostedBy": {
					"description": "Distinguishes between datasets hosted within the organization describing the data, or via another organization",
					"type": "string",
					"enum": ["Internal", "External"]
				},
				"briefDescription": {
					"description": "Short description of the dataset or project",
					"type": "string"
				},
				"briefGenealogy": {
					"description": "Notes about how the dataset came to be, if it was inferred from or combined from other datasets",
					"type": "string"
				},
				"fullNotes": {
					"description": "Full description of the dataset or project",
					"type": "string"
				},
				"includesDataTypes": {
					"description": "Optional field for denoting data types, to power interface faceting",
					"type": "array",
					"items": {
						"type": "string",
						"minItems": 1,
						"enum": ["Polygon", "Raster", "Line", "Point", "MultiPolygon", "MultiLineString", "MultiPoint", "Undetermined"]
					}
				},
				"importantAttributes": {
					"description": "List of important attributes to preview dataset contents prior to download",
					"type": "array",
					"items": {
						"type": "string"
					}
				},
				"bboxPreview": {
					"description": "Information about the dataset's coordinate system and geographic extent",
					"type": "object",
					"properties": {
						"previewSRS": {
							"description": "EPSG code",
							"type": "string",
							"const": "EPSG:4326"
						},
						"lowerCorner": {
							"type": "array",
							"description": "Lower corner coordinates",
							"items": {
								"type": "number",
								"examples": [-71.181891, 42.330256]
							}
						},
						"upperCorner": {
							"type": "array",
							"description": "Upper corner coordinates",
							"items": {
								"type": "number",
								"examples": [-71.181891, 42.330256]
							}
						}
					}
				},
				"accessCondition": {
					"description": "Notes about access conditions, specifically relating to copyright and use restrictions",
					"type": "string",
					"examples": ["This item is in the public domain. There are no restrictions on use.", "Access restriction details"]
				},
				"collectionItems": {
					"description": "If the record describes a collection, this field is used to link to collection items",
					"type": "array",
					"items": {
						"type": "string",
						"format": "uri"
					}
				}
			},
			"required": ["$id", "title", "recordType", "hostedBy", "briefDescription", "accessCondition"]
		},
		"dataEndpoints": {
			"description": "Points to direct access to the dataset",
			"type": "array",
			"items": [{
				"type": "object",
				"properties": {
					"$id": {
						"description": "Unique ID",
						"type": "string",
						"format": "uri"
					},
					"relatedResourceURL": {
						"description": "Direct data access endpoint",
						"type": "string"
					},
					"format": {
						"description": "Data format. Examples - 'Shapefile', 'GeoPackage', 'GeoJSON', 'GeoTIFF', 'CSV', 'Web Tiles', 'Web Service'",
						"type": "string",
						"example": ["Shapefile", "GeoPackage", "GeoJSON", "GeoTIFF", "CSV", "Web Tiles", "Web Service"]
					},
					"source": {
						"description": "Data source",
						"type": "string"
					},
					"accessedViaURL": {
						"description": "If the data has a record on a data portal containing additional contextual information, link to that record here",
						"type": "string"
					},
					"title": {
						"description": "Resource title",
						"type": "string"
					},
					"suggestedEntryPoint": {
						"description": "Optional front-end flag to indicate resources with which someone just entering the data should get started",
						"type": "boolean"
					}
				}
			}],
			"required": ["$id", "relatedResourceURL", "format", "source"]
		},
		"subjectTagging": {
			"description": "Topic tags to facilitate search and filtering",
			"type": "object",
			"properties": {
				"thematic": {
					"description": "Used to power interface filtering by theme",
					"type": "object",
					"properties": {
						"LCSH": {
							"description": "Library of Congress Subject Headings",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"authority": {
										"type": "string",
										"const": "LCSH"
									},
									"authorityID": {
										"type": "string",
										"const": "http://id.loc.gov/authorities/subjects.html"
									},
									"language": {
										"type": "string",
										"const": "eng"
									},
									"subjectTag": {
										"type": "string",
										"examples": ["Administrative and political divisions", "National Highway System", "Census districts"]
									}
								},
								"required": ["authority", "authorityID", "language", "subjectTag"]
							}]
						},
						"ISO19115": {
							"type": "array",
							"description": "ISO9115 Topic Categories",
							"items": [{
								"type": "object",
								"properties": {
									"authority": {
										"type": "string",
										"const": "ISO19115TopicCategory"
									},
									"authorityID": {
										"type": "string",
										"const": "http://www.isotc211.org/2005/resources/Codelist/gmxCodelists.xml#MD_TopicCategoryCode"
									},
									"language": {
										"type": "string",
										"const": "eng"
									},
									"valueID": {
										"type": "string",
										"examples": ["transportation", "boundaries"]
									},
									"subjectTag": {
										"type": "string",
										"examples": ["Transportation", "Boundaries"]
									}
								},
								"required": ["authority", "authorityID", "language", "valueID", "subjectTag"]
							}]
						},
						"otherSubjectAuthorities": {
							"description": "Place to tag subjects based on authorities not formally encoded in this schema",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"authorityName": {
										"description": "Define the authority",
										"type": "string"
									},
									"authorityID": {
										"description": "Link to the controlled terms",
										"type": "string",
										"format": "uri"
									},
									"language": {
										"description": "Note the language",
										"type": "string"
									},
									"subjectTag": {
										"description": "Enter tag",
										"type": "string"
									}
								}
							}]
						},
						"freeSubjectTags": {
							"description": "Place for subject tags that don't conform to a defined authority",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"language": {
										"description": "Note the language",
										"type": "string"
									},
									"subjectTag": {
										"description": "Enter tag",
										"type": "string"
									}
								},
								"required": ["subjectTag"]
							}]
						}
					}
				},
				"geographic": {
					"type": "object",
					"description": "Used to power interface filtering by geography",
					"properties": {
						"geonames": {
							"description": "The GeoNames geographical database covers all countries and contains over eleven million placenames that are available for download free of charge.",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"authority": {
										"type": "string",
										"const": "geonames"
									},
									"authorityID": {
										"type": "string",
										"const": "http://www.geonames.org/ontology#"
									},
									"language": {
										"type": "string",
										"const": "eng"
									},
									"valueID": {
										"type": "string",
										"examples": ["http://sws.geonames.org/4931353"]
									},
									"placeTag": {
										"type": "string",
										"examples": ["Brighton"]
									}
								},
								"required": ["authority", "authorityID", "language", "valueID", "placeTag"]
							}]
						},
						"EPSG": {
							"description": "EPSG Geodetic Parameter Dataset (also EPSG registry) is a public registry of geodetic datums, spatial reference systems, Earth ellipsoids, coordinate transformations and related units of measurement. Each entity is assigned an EPSG code between 1024-32767.",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"authority": {
										"type": "string",
										"const": "EPSG"
									},
									"authorityID": {
										"type": "string",
										"examples": ["http://opengis.net/def/crs/EPSG/0/4326"]
									},
									"epsgTag": {
										"type": "string",
										"examples": ["EPSG:4326"]
									}
								},
								"required": ["authority", "authorityID", "epsgTag"]
							}]
						}
					},
					"required": ["geonames"]
				},
				"temporal": {
					"type": "object",
					"description": "Used to power interface filtering by date",
					"properties": {
						"temporalNotes": {
							"description": "Used to provide clarity about the temporal details of the dataset",
							"type": "string"
						},
						"temporalRepresentation": {
							"description": "Place to indicate the temporal period the data represents. For instance, if a dataset was published in 2019 but the data itself describes life as it were in 1995, aspects described in temporalRepresentation should point to 1995.",
							"type": "object",
							"properties": {
								"encoding": {
									"type": "string",
									"description": "Observes: https://www.w3.org/TR/NOTE-datetime",
									"const": "https://www.w3.org/TR/NOTE-datetime"
								},
								"temporalType": {
									"description": "This field indicates whether the temporal extent of this dataset is expressed as a singular date, or as a range.",
									"type": "string",
									"enum": ["singularExpression", "temporalRange"]
								},
								"singularExpressionValue": {
									"description": "Use this field if the temporal representation is best described as a singular date or year",
									"type": "string"
								},
								"startRangeValue": {
									"description": "Use these fields if the temporal representation is best described as a range",
									"type": "string"
								},
								"endRangeValue": {
									"description": "Use these fields if the temporal representation is best described as a range",
									"type": "string"
								}
							}
						},
						"temporalPublication": {
							"description": "Used to indicate details about when the data was published.  Any other date-based information can be added in `temporalNotes` or `temporalRepresentation` for clarity.",
							"type": "object",
							"properties": {
								"encoding": {
									"type": "string",
									"description": "Observes: https://www.w3.org/TR/NOTE-datetime",
									"const": "https://www.w3.org/TR/NOTE-datetime"
								},
								"temporalType": {
									"description": "This field indicates whether the temporal extent of this dataset's publication date is expressed as a singular date, or as a range.",
									"type": "string",
									"enum": ["singularExpression", "temporalRange"]
								},
								"singularExpressionValue": {
									"description": "Use this field if the publication date is best described by a singluar date or year",
									"type": "string"
								},
								"startRangeValue": {
									"description": "Use these fields if the publication date is best described as a range",
									"type": "string"
								},
								"endRangeValue": {
									"type": "string",
									"description": "Use these fields if the publication date is best described as a range"
								}
							}
						}
					}
				}
			}
		},
		"dataBiography": {
			"description": "Background information about the who, what, where, why and how the data came to be, designed to place focus on human lives. If information about these fields is not available, these prompts can be used as conversation starters about why this information wasn't previously considered.",
			"type": "object",
			"properties": {
				"sampling": {
					"description": "Notes on the availability and quality of information about sampling characteristics of the dataset. This can include the dataset's universe, sample size, known sampling problems, and margin of error.",
					"type": "object",
					"properties": {
						"notes": {
							"description": "Notes on the availability and quality of information about sampling characteristics of the dataset. This can include the dataset's universe, sample size, known sampling problems, and margin of error. Notes should describe the extent of usefulness of the linked documentation in 'relatedResourceURL'.",
							"type": "string"
						},
						"relatedResourceURL": {
							"description": "Links to supporting documentation",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"$id": {
										"description": "Unique resource link",
										"type:": "string",
										"format": "uri"
									},
									"title": {
										"description": "Resource title",
										"type": "string"
									},
									"publisher": {
										"description": "Resource publisher",
										"type": "string"
									},
									"datePublished": {
										"description": "Resource publication date",
										"type": "string",
										"format": "date"
									}
								}
							}]
						},
						"healthCheckScore": {
							"description": "Ranking of available context 1-5. Value 1 (unhealthy) - 5 (healthy)",
							"type": "integer"
						},
						"healthCheckQualifier": {
							"description": "Notes describing the reason for the given health check score",
							"type": "string"
						}
					}
				},
				"privacy": {
					"description": "Notes on the availability and quality of information about privacy characteristics. This can include notes about the dataset's unit of aggregation, known issues with aggregation methods, the degree to which participants consented to the data being collected about them, the degree to which the data was anonymized, and any known privacy issues.",
					"type": "object",
					"properties": {
						"notes": {
							"description": "Notes on the availability and quality of information about privacy characteristics. This can include notes about the dataset's unit of aggregation, known issues with aggregation methods, the degree to which participants consented to the data being collected about them, the degree to which the data was anonymized, and any known privacy issues. Notes should describe the extent of usefulness of the linked documentation in 'relatedResourceURL'.",
							"type": "string"
						},
						"relatedResourceURL": {
							"description": "Links to supporting documentation",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"$id": {
										"description": "Unique resource link",
										"type:": "string",
										"format": "uri"
									},
									"title": {
										"description": "Resource title",
										"type": "string"
									},
									"publisher": {
										"description": "Resource publisher",
										"type": "string"
									},
									"datePublished": {
										"description": "Resource publication date",
										"type": "string",
										"format": "date"
									}
								}
							}]
						},
						"healthCheckScore": {
							"description": "Ranking of available context 1-5. Value 1 (unhealthy) - 5 (healthy)",
							"type": "integer"
						},
						"healthCheckQualifier": {
							"description": "Notes describing the reason for the given health check score",
							"type": "string"
						}
					}
				},
				"dataCollection": {
					"description": "Notes on the availability and quality of information about data collection characteristics. This can include notes about data collection start and end dates, those involved in conception, design and planning of the data structuring and collection, who was included in the planning and collecting processes, who were the data collectors, were those who the data is about involved in collecting the data and conceiving of the dataset structure, design and implementation? What collection methods were used, were there any extenuating circumstances around collection that may have impacted the quality of the results? Are there any risks for miscounts?",
					"type": "object",
					"properties": {
						"notes": {
							"description": "Notes on the availability and quality of information about data collection characteristics. This can include notes about data collection start and end dates, those involved in conception, design and planning of the data structuring and collection, who was included in the planning and collecting processes, who were the data collectors, were those who the data is about involved in collecting the data and conceiving of the dataset structure, design and implementation? What collection methods were used, were there any extenuating circumstances around collection that may have impacted the quality of the results? Are there any risks for miscounts? Notes should describe the extent of usefulness of the linked documentation in 'relatedResourceURL'.",
							"type": "string"
						},
						"relatedResourceURL": {
							"description": "Links to supporting documentation",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"$id": {
										"description": "Unique resource link",
										"type:": "string",
										"format": "uri"
									},
									"title": {
										"description": "Resource title",
										"type": "string"
									},
									"publisher": {
										"description": "Resource publisher",
										"type": "string"
									},
									"datePublished": {
										"description": "Resource publication date",
										"type": "string",
										"format": "date"
									}
								}
							}]
						},
						"healthCheckScore": {
							"description": "Ranking of available context 1-5. Value 1 (unhealthy) - 5 (healthy)",
							"type": "integer"
						},
						"healthCheckQualifier": {
							"description": "Notes describing the reason for the given health check score",
							"type": "string"
						}
					}
				},
				"funding": {
					"description": "Notes on the availability and quality of information about funding characteristics.",
					"type": "object",
					"properties": {
						"notes": {
							"description": "Notes on the availability and quality of information about funding characteristics. Notes should describe the extent of usefulness of the linked documentation in 'relatedResourceURL'.",
							"type": "string"
						},
						"relatedResourceURL": {
							"description": "Links to supporting documentation",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"$id": {
										"description": "Unique resource link",
										"type:": "string",
										"format": "uri"
									},
									"title": {
										"description": "Resource title",
										"type": "string"
									},
									"publisher": {
										"description": "Resource publisher",
										"type": "string"
									},
									"datePublished": {
										"description": "Resource publication date",
										"type": "string",
										"format": "date"
									}
								}
							}]
						},
						"healthCheckScore": {
							"description": "Ranking of available context 1-5. Value 1 (unhealthy) - 5 (healthy)",
							"type": "integer"
						},
						"healthCheckQualifier": {
							"description": "Notes describing the reason for the given health check score",
							"type": "string"
						}
					}
				},
				"potentialImpact": {
					"description": "Notes related to the availability and quality of information available about the potential positive or negative impacts of the dataset on those who are represented in the dataset",
					"type": "object",
					"properties": {
						"notes": {
							"description": "Notes related to the availability and quality of information about potential harm risk, or those who may have a stake in the data's quality. Notes should describe the extent of usefulness of the linked documentation in 'relatedResourceURL'.",
							"type": "string"
						},
						"relatedResourceURL": {
							"description": "Links to supporting documentation",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"$id": {
										"description": "Unique resource link",
										"type:": "string",
										"format": "uri"
									},
									"title": {
										"description": "Resource title",
										"type": "string"
									},
									"publisher": {
										"description": "Resource publisher",
										"type": "string"
									},
									"datePublished": {
										"description": "Resource publication date",
										"type": "string",
										"format": "date"
									}
								}
							}]
						},
						"healthCheckScore": {
							"description": "Ranking of available context 1-5. Value 1 (unhealthy) - 5 (healthy)",
							"type": "integer"
						},
						"healthCheckQualifier": {
							"description": "Notes describing the reason for the given health check score",
							"type": "string"
						}
					}
				}
			}
		},
		"resourceConstellation": {
			"description": "Datasets or data projects are often not useful on their own. Often, we need an entire constellation of supporting resources to make sense of the data we find. The information in this section can be added to as more learning materials become available. ",
			"type": "object",
			"properties": {
				"codebooks": {
					"description": "Reference to any codebooks or data dictionaries intended to aid interpretation of the data's attributes or field names",
					"type": "array",
					"items": [{
						"type": "object",
						"properties": {
							"$id": {
								"description": "Unique identifier",
								"type:": "string",
								"format": "uri"
							},
							"title": {
								"description": "Resource title",
								"type": "string"
							},
							"maintainedBy": {
								"description": "Person or organization responsible for maintaining and providing access to the codebook",
								"type": "string"
							},
							"notes": {
								"description": "Place to put helpful notes or information about finding, using or interpreting the codebook",
								"type": "string"
							},
							"suggestedEntryPoint": {
								"description": "Optional front-end flag to indicate resources with which someone just entering the data should get started",
								"type": "boolean"
							},
							"importantContext": {
								"description": "Optional front-end flag to indicate context fields to elevate to high prominence in a discovery environment",
								"type": "boolean"
							}
						},
						"required": ["$id", "title", "maintainedBy"]
					}]
				},
				"publishedWorks": {
					"description": "Reference to related blogs, articles, books, data visualizations or other published research based on this data.",
					"type": "array",
					"items": [{
						"type": "object",
						"properties": {
							"$id": {
								"description": "Unique identifier",
								"type:": "string",
								"format": "uri"
							},
							"title": {
								"description": "Resource title",
								"type": "string"
							},
							"author": {
								"description": "Resource author",
								"type": "string"
							},
							"publisher": {
								"description": "Resource publisher",
								"type": "string"
							},
							"datePublished": {
								"description": "Resource publication date",
								"type": "string",
								"format": "date"
							},
							"notes": {
								"description": "Place to note how this resource is relelvant",
								"type": "string"
							},
							"suggestedEntryPoint": {
								"description": "Optional front-end flag to indicate resources with which someone just entering the data should get started",
								"type": "boolean"
							},
							"importantContext": {
								"description": "Optional front-end flag to indicate context fields to elevate to high prominence in a discovery environment",
								"type": "boolean"
							}
						}
					}]
				},
				"tutorials": {
					"description": "Reference to guides, tutorials, Jupyter notebooks, or any resource with people-centered instructions on how to work with, use and understand this data",
					"type": "array",
					"items": [{
						"type": "object",
						"properties": {
							"$id": {
								"description": "Unique identifier",
								"type:": "string",
								"format": "uri"
							},
							"title": {
								"description": "Resource title",
								"type": "string"
							},
							"author": {
								"description": "Resource author",
								"type": "string"
							},
							"datePublished": {
								"description": "Resource publication date",
								"type": "string",
								"format": "date"
							},
							"notes": {
								"description": "Place to note how this resource is relelvant",
								"type": "string"
							},
							"suggestedEntryPoint": {
								"description": "Optional front-end flag to indicate resources with which someone just entering the data should get started",
								"type": "boolean"
							},
							"importantContext": {
								"description": "Optional front-end flag to indicate context fields to elevate to high prominence in a discovery environment",
								"type": "boolean"
							}
						},
						"required": ["$id", "title"]
					}]
				},
				"supplementaryFiles": {
					"description": "Points to files that complement the data at hand",
					"type": "array",
					"items": [{
						"type": "object",
						"properties": {
							"$id": {
								"description": "Unique identifier",
								"type:": "string",
								"format": "uri"
							},
							"title": {
								"description": "Resource title",
								"type": "string"
							},
							"datePublished": {
								"description": "Resource publication date",
								"type": "string",
								"format": "date"
							},
							"notes": {
								"description": "Place to note how this resource is relelvant",
								"type": "string"
							},
							"suggestedEntryPoint": {
								"description": "Optional front-end flag to indicate resources with which someone just entering the data should get started",
								"type": "boolean"
							},
							"importantContext": {
								"description": "Optional front-end flag to indicate context fields to elevate to high prominence in a discovery environment",
								"type": "boolean"
							}
						},
						"required": ["$id", "title"]
					}]
				}
			}
		},
		"dataLifecycle": {
			"description": "Human beings often make changes to data during the process of working with and making sense of it. The context in this section is aimed at painting a fuller picture of the actions taken by other humans, in order to help someone understand how they might want to use the data. ",
			"type": "object",
			"properties": {
				"acquisition": {
					"description": "Used to describe where the data originated from, if it was derived from other from other data sources",
					"type": "object",
					"properties": {
						"ingredients": {
							"description": "Used to provide relational links and notes about relationship quality to the 'ingredient' datasets this dataset was created from",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"$id": {
										"type": "string",
										"format": "uri",
										"description": "Each item object in the array refers to a unique ingredient used to create the new dataset. This field denotes the ID for the input ingredient dataset."
									},
									"type": {
										"description": "If this ingredient alone was improved upon to create new new dataset, select 'linearPredecessor'. If it was combined with other datasets to create the new dataset, select 'compositeIngredient'. This distinction powers the way the project is presented to the user in the catalog interface.",
										"type": "string",
										"enum": ["linearPredecessor", "compositeIngredient"]
									},
									"notes": {
										"type": "string",
										"description": "This field is used to qualify the data genealogy between the stated datasets, if it is not clear"
									},
									"suggestedEntryPoint": {
										"description": "Optional front-end flag to indicate resources with which someone just entering the data should get started",
										"type": "boolean"
									}
								}
							}]
						}
					}
				},
				"description": {
					"description": "Information about how the dataset has been described, who described it, based on what information",
					"type": "object",
					"properties": {
						"language": {
							"description": "Observes ISO 639-2",
							"type": "object",
							"properties": {
								"authority": {
									"type": "string",
									"description": "https://www.loc.gov/standards/iso639-2/php/code_list.php",
									"const": "https://www.loc.gov/standards/iso639-2/php/code_list.php"
								},
								"value": {
									"description": "Appropriate code",
									"type": "string"
								}
							}
						},
						"contextProvider": {
							"description": "Person describing this dataset or project",
							"type": "object",
							"properties": {
								"name": {
									"description": "Person's name",
									"type": "string"
								},
								"relationshipToData": {
									"type": "string",
									"description": "Person's relationship to dataset or project"
								}
							}
						},
						"contextOnBehalfOf": {
							"description": "If the person filling out the context is different than the person responsbile for the creation or processing of the dataset or project, note that here",
							"type": "object",
							"properties": {
								"name": {
									"description": "Person's name",
									"type": "string"
								},
								"relationshipToData": {
									"type": "string",
									"description": "Person's relationship to dataset or project"
								}
							}
						},
						"contextPublicationDate": {
							"description": "Date this context was first made available",
							"type": "string",
							"format": "date"
						},
						"metadataSchema": {
							"type": "object",
							"description": "Links out to full LMEC Data Description Schema",
							"properties": {
								"schemaName": {
									"type": "string",
									"const": "LMEC Data Description Schema"
								},
								"$id": {
									"type": "string",
									"description": "https://github.com/nblmc/Data-Context/blob/master/schema.json",
									"const": "https://github.com/nblmc/Data-Context/blob/master/schema.json"
								}
							}
						}
					},
					"required": ["language", "contextProvider", "contextPublicationDate"]
				},
				"maintenance": {
					"type": "object",
					"description": "Information about who is responsible for updating and maintaining the dataset",
					"properties": {
						"officialMaintainer": {
							"description": "Person or organization responsible for maintaining the dataset",
							"type": "string"
						},
						"maintenanceFrequency": {
							"description": "Regularity with which this dataset is updated. Example values could be 'None', 'Every Monday at Midnight', 'Annually', 'Unknown'",
							"type": "string"
						},
						"typicalUpdates": {
							"description": "If this data is regularly updated, describe the typical nature of the changes",
							"type": "string"
						},
						"contextMissing": {
							"description": "Optional front-end flag to indicate context fields for which the metadata writer believes context would have been helpful, but no information is available or easily discoverable via original data providers",
							"type": "boolean"
						}
					},
					"required": ["officialMaintainer"]
				},
				"manipulation": {
					"description": "Used to refer to documentation provided by the person who manipulated the data, about the human choices involved in processing this dataset. Contributors of READMEs or other tutorial-based docs should be encouraged to format their descriptions in a way that someone who has never encountered this data before could reproduce the process. Where possible, contributors should write step-by-step explanations in chronological order, providing audio or image supplements for additional clarity and accessibility.",
					"type": "object",
					"properties": {
						"choices": {
							"description": "Links out to READMEs, tutorials or other documentation contributed by the data processor",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"relatedResourceURL": {
										"description": "Direct link to access the process documentation",
										"type": "string"
									},
									"format": {
										"type": "string",
										"description": "Place to denote resource type",
										"examples": ["Blog", "Article", "Guide", "Tutorial", "Jupyter Notebook", "Plain-text README"]
									},
									"title": {
										"description": "Name for the record",
										"type": "string"
									},
									"author": {
										"description": "Name of person who authored the record",
										"type": "string"
									},
									"suggestedEntryPoint": {
										"description": "Optional front-end flag to indicate resources with which someone just entering the data should get started",
										"type": "boolean"
									},
									"contextMissing": {
										"description": "Optional front-end flag to indicate context fields for which the metadata writer believes context would have been helpful, but no information is available or easily discoverable via original data providers",
										"type": "boolean"
									},
									"importantContext": {
										"description": "Optional front-end flag to indicate context fields to elevate to high prominence in a discovery environment",
										"type": "boolean"
									}
								}
							}]
						},
						"tools": {
							"description": "Links out to scripts or other tools created by the processor to aid in the data processing",
							"type": "array",
							"items": [{
								"type": "object",
								"properties": {
									"relatedResourceURL": {
										"type": "string"
									},
									"format": {
										"type": "string",
										"description": "Place to denote resource type",
										"examples": ["Python Script"]
									},
									"title": {
										"description": "Name for the record",
										"type": "string"
									},
									"author": {
										"description": "Name of person who authored the record",
										"type": "string"
									},
									"suggestedEntryPoint": {
										"description": "Optional front-end flag to indicate resources with which someone just entering the data should get started",
										"type": "boolean"
									},
									"contextMissing": {
										"description": "Optional front-end flag to indicate context fields for which the metadata writer believes context would have been helpful, but no information is available or easily discoverable via original data providers",
										"type": "boolean"
									},
									"importantContext": {
										"description": "Optional front-end flag to indicate context fields to elevate to high prominence in a discovery environment",
										"type": "boolean"
									}
								}
							}]
						},
						"unknown": {
							"description": "Mark true if processing was performed but details about the process are unknown.",
							"type": "boolean"
						}	
					}
				}
			},
			"required": ["description", "maintenance"]
		}
	},
	"required": ["coreCitation", "subjectTagging", "dataLifecycle"]
}